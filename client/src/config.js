// Basic configuration for API URLs
// Hardcoded to Render Backend to ensure it works in Production
// Localhost can also use this since Backend has CORS enabled

export const API_BASE_URL = 'https://tour-travel-website-vt8y.onrender.com/api'; // Forced Update 2

export default API_BASE_URL;
